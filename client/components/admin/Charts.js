"use client";
import React from "react";

const Charts = () => {
  return (
    <section style={{padding: 12, border: '1px solid #eee', borderRadius: 6}}>
      {/* Placeholder for charts — integrate charting library (Chart.js / Recharts) as needed */}
      <div style={{height: 200, display: 'flex', alignItems: 'center', justifyContent: 'center', color: '#888'}}>Charts will render here</div>
    </section>
  );
};

export default Charts;
